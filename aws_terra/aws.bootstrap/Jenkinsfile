pipeline {
    agent any
    stages {
        stage('Bulid') {
            steps {
              script {
                    echo "Setup EC2." 
                }  
            }
        }
        
        stage('Deploy EC2 ') {
            parallel {
                stage ('Deploy to AWS'){
                    echo "deploying to aws" 
                }  
            }
        }
    }
}